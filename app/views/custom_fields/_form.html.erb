<%= form_with(model: [@custom_object, custom_field], local: true) do |form| %>
    <% if custom_field.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(custom_field.errors.count, "error") %> prohibited this custom_field from being saved:</h2>
        <ul>
          <% custom_field.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name %>
    </div>
  
    <div class="field">
      <%= form.label :field_type %>
      <%= form.text_field :field_type %>
    </div>
  
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>  